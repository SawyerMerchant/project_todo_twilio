<%= form_for @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :due_date %>
  <%= f.date_field :due_date, min: Time.now %>

  <%= f.label :done %>
  <%= f.check_box :done %>
  <%= f.submit "Submit" %>
<% end %>
